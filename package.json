{
  "name": "youtube-filter",
  "version": "0.2.0",
  "private": true,
  "description": "YouTube専用のAnime4Kアップスケーラー兼カラーレンジ変換フィルター",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check src/content.js && node --check src/renderer.js && node --check src/player-settings.js && node --check src/popup.js && node --check build.mjs && node --check test/validate.mjs",
    "test": "node test/validate.mjs"
  },
  "dependencies": {
    "anime4k-webgpu": "1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
