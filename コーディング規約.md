# コーディング規約

## JavaScript

- ECMAScript Modulesを使用し、`import`と`export`を用いる。
- インデントはスペース2個、文末はセミコロン、文字列はダブルクォートを基本とする。
- 変数と関数はcamelCase、定数は既存コードに合わせてUPPER_SNAKE_CASE、クラスはPascalCaseとする。
- 状態や許容値の定義を複製せず、既存のスキーマまたは専用モジュールを唯一の定義元にする。
- 外部入力、Chromeメッセージ、保存済み設定は使用前に検証する。
- 非同期処理は停止後に状態を更新しないよう、世代番号、破棄フラグ、Promiseの同一性などで競合を防ぐ。
- 早期returnが読みやすい短い条件では中括弧の省略を許容し、既存ファイルの形式を優先する。

## ファイル構成

- `src/`には生成元の実装、`public/`には静的配布素材、`test/`にはNode.jsで実行可能なテストを置く。
- `dist/`は生成物のため直接編集しない。
- 新しい責務は既存モジュールへ無理に詰め込まず、単独で検証可能なら小さなモジュールへ分離する。

## テスト

- 不具合修正には、可能な限り失敗条件を再現する回帰テストを追加する。
- テストは外部ネットワークや実GPUへ依存させず、境界をモックして決定的に実行できるようにする。
- 実GPUやYouTubeのDOMが必要な確認は、自動テストと区別してPRへ記載する。
